#include <stdio.h>
/**
 * main - Entry point
 * @argc: number of arguments
 * @argv: argument vector
 *
 * Return: 0 if success.
 */
int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
